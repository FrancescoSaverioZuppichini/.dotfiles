import requests


def __init__(
    self,
    symbol: str,
    # TODO: when I first wrote blah, WTF .. fix this shit
    exchange_to: str,
):
    '''[summary]

    :param symbol: [description]
    :type symbol: str
    :param WTF: [description]
    :type WTF: [type]
    '''
    pass

def insert_release_info(release_info: Dict[str, str], latest_release: List[str]) -> Optional(int):
    '''[summary]

    :param release_info: [description]
    :type release_info: Dict[str, str], latest_release
    :return: [description]
    :rtype: [type]
    '''
    return x

def foo(bar):
    x = """hello"""
    return x


def no_return(value):
    print(value)


def function(a, b=1):

    return a

    def function(a):
        raise x


async def function2(arg: int, kwarg="abc") -> int:
    requests.get

    abc

    raise this
    return pee


def multi_line_func(arg,
                    arg2,
                    arg3,
                    kwarg=""):
    pass


def get_db_size(array, bytekey, datatypekey):
    for eachdb in array:
        seq, length = [x[bytekey]
                       for x in eachdb], [x[datatypekey] for x in eachdb]
        floatseq = map(float, seq)
        idx = floatseq.index(max(floatseq))
        lastByte = int(max(floatseq))+(offsets[length[idx]])
        print(eachdb)
        """
        print "seq:%s"%type(seq)
        print seq
        """
    return lastByte


def generateEmbed(messageAuthor, title, description, footerText, imageURL="abc", colour=0x00ff00):
    """
    generateEmbed [summary]

    [extended_summary]

    Args:
        messageAuthor ([type]): [description]
        title ([type]): [description]
        description ([type]): [description]
        footerText ([type]): [description]
        imageURL (str, optional): [description]. Defaults to "abc".
        colour (hexadecimal, optional): [description]. Defaults to 0x00ff00.

    Raises:
        poop: [description]
        poop2: [description]

    Returns:
        [type]: [description]

    Yields:
        [type]: [description]
    """
    raise poop


def generateEmbed(messageAuthor):
    raise poop2
    yield poop
    return 3
